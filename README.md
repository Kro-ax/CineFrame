# CineFrame
CineFrame to make the planning processes for content creation easier and more enjoyable. 

PIP install the relevant dependencies.
To run, enter your OpenAI API key in the code, cd to the correct folder and run this command "python -m streamlit run CineFrame.py"

